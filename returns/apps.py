from django.apps import AppConfig


class ReturnsConfig(AppConfig):
    """Configuration de l'application de retours et remboursements."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "returns"